@using OSS.Infrastructure.Web.WebExtentions
@{
    var isPjax = Context.Request.IsPjax();
    Layout = isPjax ? "_LayoutPjax" : "_Layout";
    //ViewBag.IsWap = MemberShiper.AppAuthorize.AppClient == AppClientType.Wap;

    //var secs = AppInfoUtil.IsDev ? 5 : 60 * 60;

    //Context.Response.Headers[HeaderNames.CacheControl] = $"public,max-age={secs}";
    //Context.Response.Headers[HeaderNames.ETag] = AppWebInfoUtil.GetPjaxTagWithTenant();

    //var pVer = Context.Request.Headers["X-PJAX-Ver"].FirstOrDefault();
    //if (!string.IsNullOrEmpty(pVer) && AppInfoUtil.AppVersion != pVer)
    //{
    //    Context.Response.Headers["X-PJAX-Ver"] = AppInfoUtil.AppVersion;
    //}
}