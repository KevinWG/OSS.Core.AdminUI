@page

@using OSS.Infrastructure.Utils
@using OSS.Infrastructure.Web.Filters.Util

@model OSS.Core.AdminUI.Pages.Portal.LoginModel
@{
      ViewData["Title"] = "登录页面";
    Layout = "_PortalLayout";
}

<div class="row justify-content-center" id="login-component">
    <div class="col-md-8">
        <div class="card-group">
            <div class="card p-4">
                <div class="card-body">
                    <h1>Login</h1>
                    <p class="text-muted">Sign In to your account</p>
                    <div class="input-group mb-3">
                        <div class="input-group-prepend">
                            <span class="input-group-text">
                                <i class="icon-user"></i>
                            </span>
                        </div>
                          <input class="form-control" type="text"  v-model="login.user_name" name="user_name" placeholder="输入用户名！">
                                          
                    </div>
                    <div class="input-group mb-4">
                        <div class="input-group-prepend">
                            <span class="input-group-text">
                                <i class="icon-lock"></i>
                            </span>
                        </div>

                        <input class="form-control" type="password"  v-model="login.password" name="password" placeholder="请输入密码！">
            
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <button class="btn btn-primary px-4" type="button">Login</button>
                        </div>
                        <div class="col-6 text-right">
                            <button class="btn btn-link px-0" type="button">Forgot password?</button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card text-white bg-primary py-5 d-md-down-none" style="width:44%">
                <div class="card-body text-center">
                    <div>
                        <h2>Sign up</h2>
                        <p>
                        </p>
                        <button class="btn btn-primary active mt-3" type="button">
                            Register Now!
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{

    <script>

     $(function() {
         OssPjaxEvents.pageLoaded = initailLogin;
    });

//初始化页面验证
function initailLogin() {
    return new Vue({
        el: "#login-component",
        data: {
            login: {
                user_name: "sss",
                password: "ss"
            }            
        },
        methods: {
            submit: function(event) {
                var self = this;
                this.$validator.validateAll().then(function(vr) {
                    if (!vr)
                        return;

                    var data = self.login;
                    var url = "/api/portal/codelogin";

                    // OssApi.postBtn($(event.target), url, data,true)
                    //     .done(function(res) {
                    //        if (res.isOK) {
                    //             goTo(!res.r_url ? "/plat/list" : res.r_url);
                    //        } else {
                    //            OssTips.showTipError(res.msg);
                    //        }
                    //    }); 

                });
            }
        },
        created: function() {                 
        }
    });
}
    </script>
 
}
